# 🎯 实习成果展示 — 演示前准备清单

> 创建日期：2026-07-14 ｜ 目标演示日期：本周内
> 最后更新：2026-07-16 21:15（第9周 Day3）
> 使用方式：逐项打勾，确保演示前每一项都是 ✅

---

## 📋 今日（7/16）成果摘要 🎉 NVD 预热攻克 + 全链路打通

| 类别 | 完成项 | 状态 |
|------|--------|------|
| **NVD 预热** | **成功！** datafeed 本地镜像方案，缓存 250MB，`check-nvd.bat` 输出 [OK] | ✅ 3/3 |
| 根因确诊 | NVD API 直连：curl 200 但 Java 客户端 520 → **Cloudflare 按 TLS 指纹拦截，换 VPN 节点无效** | ✅ |
| 替代方案 | mirror.cveb.in 全量数据(212MB) → 规范化时间戳 → 本地 8888 伺服 → 引擎 2 分钟导入 | ✅ |
| 上传扫描 | 三项目全部 COMPLETED：PetClinic 149依赖/18漏洞、DVBA 27依赖/61漏洞、Nacos 22依赖/32漏洞 | ✅ |
| 仪表盘 | totalProjects=3、漏洞 111（CRITICAL 23 / HIGH 55 / MEDIUM 33 / LOW 0）、许可证 57 | ✅ |
| 报告修复 | Excel/PDF 下载 400 → 按需生成打通（3任务×3格式全部 200） | ✅ |
| **离线模式** | `NVD_AUTO_UPDATE=false` 扫描完全离线（无 VPN/Key/镜像），Nacos 复扫 COMPLETED | ✅ |
| Git 清理 | .gitignore + 解除 target 跟踪 + 脚本 Key 外部化 + 3 个 commit，全历史无 Key 泄露 | ✅ 3/3 |
| 测试 | 后端 67/67 通过，零回归 | ✅ |

### 7/16 新增代码修复（均已验证）

| 修复 | 文件 | 说明 |
|------|------|------|
| NVD datafeed 配置 | `ScanEngineService` / `application.yml` | 新增 `nvd-datafeed-url`（`{0}`=年份占位），绕开 API 直连 |
| NVD 离线开关 | 同上 | 新增 `nvd-auto-update`（演示日设 false，扫描零联网） |
| OSS Index 禁用 | 同上 | 在线分析器无凭据必失败，默认关闭（`ossindex-enabled`） |
| .NET Assembly 禁用 | 同上 | 本机无 dotnet 时初始化异常导致扫描失败，直接禁用 |
| Excel 报告修复 | `pom.xml` | ① 移除 xmlbeans 排除（NoClassDefFoundError）② commons-compress 升 1.26.2（POI 5.2.5 需 ≥1.25） |
| PDF 报告修复 | `ReportService` | DC HTML5 报告 → jsoup 转 XHTML + 移除 script，Flying Saucer 才能解析 |
| 报告按需生成 | `ReportService` | generateExcel/PdfReport 原先无调用方（永远 404）；下载时按需生成 + 0 字节残留防护 |
| check-nvd.bat | 根目录 | 检查 odc.mv.db（原 *.h2.db 永远误报）+ 100MB 阈值 |
| 辅助脚本 | `gen-cache-properties.py` / `normalize-nvd-json.py` | datafeed 镜像 cache.properties 生成 + 时间戳规范化 |

### ⚠️ 已知问题（不阻塞演示）

- 项目逻辑删除后**同名不能重建**（unique 索引含已删行）— 演示时别删项目重传同名
- `PUT /api/projects/{id}` 未实现（前端也无入口），描述改错只能删了重传
- `GET /api/tasks` 列表不含 projectName（前端只显示 projectId，不影响页面）

---

## 一、代码就绪 ✅（7/14 已完成）

- [x] **G2-02** 仪表盘漏洞按 severity 分级统计 → 新建 `DashboardController`，CRITICAL/HIGH/MEDIUM/LOW 独立显示
- [x] **G4-02** 扫描任务自动轮询 → `useTaskPolling` composable，Tasks/TaskDetail 页自动刷新
- [x] **G4-03** 前端表格分页 → Projects / Tasks / Reports 三分页全对接
- [x] **G4-01** `statusType`/`statusText` 抽取为 `useStatus` composable，4 个组件统一引用
- [x] **G4-04** 菜单项"系统设置"/"退出登录"添加 click 处理 → `ElMessage.info('功能开发中')`
- [x] **G4-05** 添加 404 路由 + `NotFound.vue`
- [x] **G3-01~03** 移除死依赖（echarts / vue-echarts / sass）和死代码（reportApi.exists/getFormats）
- [x] **G1-01** 清理 `findByNameAndIdNot` 死代码
- [x] **后端测试** 67/67 通过，零回归（7/16 复测通过）
- [x] **前端 build** 成功，无报错
- [x] **缺失端点** 补充 `GET /api/tasks` 分页列表接口 + `ScanTaskService.listTasks()`
- [x] **logo.svg** 补充占位 logo，修复前端构建失败

---

## 二、测试数据准备 ✅（7/16 完成）

### 现状

| 资源 | 状态 |
|------|------|
| 三个 ZIP 包 | ✅ 已重新打包（7/16：dvba/nacos 补入依赖 jar，源码 ZIP 扫不出依赖） |
| Web 应用测试数据 | ✅ 3 项目 + 4 任务全部 COMPLETED |
| NVD 缓存 | ✅ `data/dc-cache/odc.mv.db` ~250MB，`check-nvd.bat` [OK] |

### 演示数据快照（7/16 实测）

| 项目 | ZIP | 依赖数 | 漏洞数 | 扫描耗时 |
|------|-----|--------|--------|----------|
| Spring PetClinic | spring-petclinic.zip (61MB，含 fat jar) | 149 | 18 | ~2min |
| DVBA 漏洞靶场 | dvba.zip (33MB，7/16 重打包含 fat jar) | 27 | **61** | ~2min |
| Nacos Config Example | nacos-example.zip (17MB，7/16 重打包含 lib jars) | 22 | 32 | ~1min |

- [x] **NVD 预热完成**：缓存 250MB，[OK]
- [x] **扫描全部完成**：三个任务均 ✅ COMPLETED
- [x] **报告可下载**：3 任务 × HTML/Excel/PDF 全部 HTTP 200（7/16 修复后实测）
- [x] **仪表盘数据**：totalProjects=3，漏洞 111，四等级分布正常（LOW=0 属正常，可话术说明）
- [ ] **自动轮询生效**：浏览器实际观察「🔄 自动刷新中」（API 已验证，UI 待人工确认）
- [x] **分页生效**：projects/tasks 分页接口 total/records 正确

### 🔑 NVD 数据维护（演示后如需更新）

```bash
# 1. 启动本地镜像服务（数据在 dependency-check-web/data/nvd-mirror/，212MB 已下载）
cd dependency-check-web/data/nvd-mirror && python -m http.server 8888 --bind 127.0.0.1
# 2. 带 datafeed 启动后端（重新下载镜像数据用 curl mirror.cveb.in + normalize-nvd-json.py + gen-cache-properties.py）
NVD_DATAFEED_URL="http://127.0.0.1:8888/nvdcve-2.0-{0}.json.gz" mvn spring-boot:run
# 3. 发起任意扫描即触发增量更新
```

---

## 三、演示环境验证 🟡（7/16 API 层全部通过，UI 层待人工过一遍）

- [x] **Java 环境**：JDK 17（zulu17）
- [x] **Maven 环境**：正常
- [x] **Node 环境**：正常（Vite dev server 3000 运行中）
- [x] **端口检查**：8080 / 3000 正常监听
- [x] **后端启动**：45s 内就绪（actuator/health 200）
- [x] **前端启动**：HTTP 200
- [ ] **浏览器兼容**：Chrome / Edge 人工确认
- [x] **全流程走通**：上传 → 扫描 → 结果(75条) → 报告三格式下载 → 仪表盘（API 全验证）
- [ ] **自动轮询验证**：浏览器人工确认进度条自动更新
- [ ] **分页验证**：浏览器人工确认列表分页控件

### 🎬 演示日启动方式（离线模式，无需 VPN/Key/镜像服务）

```bash
# 方式一（推荐）：双击 start-demo.bat（已内置 NVD_AUTO_UPDATE=false，jar 已构建）
# 方式二：手动
cd dependency-check-web && NVD_AUTO_UPDATE=false mvn spring-boot:run
cd dependency-check-web/frontend && npm run dev
```

### 现场应急

| 问题 | 措施 |
|------|------|
| 端口占用 | `netstat -ano \| findstr 8080` → `taskkill /F /PID xxx` |
| 后端启动失败 | 删 `data/dependency-check.mv.db` 重试（会丢演示数据，需重新上传扫描，~5min） |
| NVD 缓存损坏 | **不要删 dc-cache 重扫**（需镜像服务）；用备份：提前拷贝 `data/dc-cache/` 一份 |
| **终极备份** | 提前录 2 分钟屏幕录像 |

---

## 四、文档补齐 ✅（4/4 完成）

- [x] **《系统用户使用手册》** — 9 章节 + FAQ + 操作速查表，7 处截图占位待补
- [x] **项目总览文档** — 9 章：背景/定位/架构/功能/数据/亮点/完成度/交付物/限制
- [x] **《依赖分析报告格式说明》** — 7 章 + 附录：生成方式/内容结构/API/存储策略/对比
- [x] **Swagger API 文档** — springdoc-openapi 2.6.0，`/swagger-ui.html` 可访问（7/16 复验 200）

**PRD 要求的 11 份文档对照**：

| # | 文档 | 状态 |
|---|------|------|
| 1 | 《OWASP Dependency-Check 本地环境搭建手册》 | ✅ 已有 |
| 2 | 《OWASP Dependency-Check 核心模块原理分析手册》 | ✅ 已有 |
| 3 | 《OWASP Dependency-Check 高级使用手册》 | 🟢 低优（上游有官方文档） |
| 4 | 《集团依赖分析工具标准配置手册》 | 🟢 低优（集团内部，非实习范围） |
| 5 | 《OWASP Dependency-Check 常见问题排查手册》 | 🟢 低优（周报含踩坑记录） |
| 6 | 《集团依赖安全与合规管理最佳实践》 | 🟢 低优（同上） |
| 7 | 《系统用户使用手册》 | ✅ **7/14 完成** |
| 8 | 《轻量依赖分析Web服务开发手册》 | 🟢 低优（内容在周报+完成计划中） |
| 9 | 《前端开发手册》 | 🟢 低优（项目说明文档 §10 已覆盖） |
| 10 | 《依赖分析报告格式说明》 | ✅ **7/14 完成** |
| 11 | 项目总览文档 | ✅ **7/14 完成** + `项目说明文档.md` 已有 |

---

## 五、演示材料准备 🔴（7/17 重点：数据已就绪，可直接截图）

- [ ] **PPT / 演示文稿**（10-15 页）
  - ✅ 7/16 逐页内容填充稿已产出：`工作任务清单与计划文件夹/PPT内容填充稿.md`（对应阿里模板 14 页，含每页标题/要点/演讲词）
  - ⏳ 待你在 PPT 模板中排版 + 贴 4 张截图（仪表盘/任务列表/HTML报告/Swagger）
  - 技术难点页新增素材：NVD Cloudflare 拦截排查（curl 200 vs Java 520 → TLS 指纹）+ datafeed 镜像方案
- [ ] **演示脚本**（逐步骤操作 + 话术）
- [ ] **架构图**（用 `项目总览文档.md` 第 3 节的 ASCII 图转 draw.io）
- [ ] **演示截图**（数据已就绪：仪表盘 111 漏洞四等级 / 任务列表 / HTML 报告 / Swagger）
- [ ] **演示视频备份**（2 分钟屏幕录像）
- [ ] **用户手册 7 处截图占位补齐**（同一批截图可复用）

---

## 六、答辩话术准备 🟡

- [ ] "为什么选这个项目？" → 集团痛点 + ODC 标准引擎 + 降低门槛
- [ ] "你做了什么vs别人做了什么？" → 独立完成全栈 + 引擎作为 library 引入
- [ ] "完成度为什么 99%？" → 核心 100%，剩余是外围打磨
- [ ] "为什么没做认证？" → 聚焦核心能力，预留扩展点
- [ ] "为什么 H2 不是 MySQL？" → 零依赖开箱即用，MyBatis-Plus 已屏蔽差异
- [ ] **"最大技术难点？" → 推荐讲 7/16 NVD 攻坚**：Cloudflare 按 TLS 指纹拦截 Java 客户端（curl 200 / Java 520 的差分定位）→ datafeed 镜像 + cache.properties 逆向 + 时间戳格式兼容三连修 → 离线可演示。备选：@Async 代理 / Zip Slip 三层 / OSS 条件降级
- [ ] "实际价值？" → 统一标准 + 降低门槛 + 完整文档
- [ ] ⚠️ 统一口径：H2 文件模式**重启不丢数据**（`jdbc:h2:file:./data/dependency-check`），ISSUES.md 中"重启丢失"指任务运行时状态（ConcurrentHashMap），两者别混

---

## 七、演示日清单 🔴

### 演示前（提前 30 分钟）
- [ ] 接电源、关无关程序
- [ ] 双击 `start-demo.bat`（或手动启动，见第三节），确认正常
- [ ] 确认仪表盘有数据（3 项目 + 111 漏洞统计）
- [ ] 打开 HTML 报告 tab 备用
- [ ] 打开 PPT / 演示脚本备用
- [ ] 清理浏览器历史

### 演示中（15-20 分钟）
- [ ] 开场：自我介绍 + 项目名称（30s）
- [ ] PPT：背景 → 架构 → 功能（5min）
- [ ] 现场演示：上传 → 扫描 → 结果 → 报告 → 仪表盘（8min）
  - 💡 现场扫描用 **nacos-example.zip**（~1min 最快）；PetClinic/DVBA 提前扫好展示结果
- [ ] 技术亮点讲解（3min）
- [ ] 总结 + Q&A（3min）

### 演示后
- [ ] 关闭服务
- [ ] 提交最终代码
- [ ] 打包交付物：代码 + 文档 + PPT

---

## 八、⚠️ 提交前必做 ✅（7/16 全部完成）

| 文件 | 问题 | 处理 |
|------|------|------|
| `application.yml` | Key 默认值 | ✅ 已还原为 `${NVD_API_KEY:}`（空默认） |
| `nvd-api.properties` | 含明文 Key | ✅ 已入 .gitignore（7/16 新建，此前**并不存在** .gitignore！），`git log -S` 全历史无泄露 |
| `pom.xml` | open-vulnerability-clients 7.1.0 | ✅ 保留，另补 commons-compress 1.26.2 |
| `nvd-warmup.bat` / `start-demo.bat` | 曾硬编码 Key | ✅ 已外部化（环境变量 / nvd-api.properties 读取） |
| `target/` 编译产物 | 曾被 Git 跟踪 | ✅ `git rm --cached` 解除 47 个文件 |

---

## 📊 进度总览（7/16 更新）

| 大类 | 总项 | 已完成 | 待完成 | 状态 |
|------|------|--------|--------|------|
| 代码就绪 | 12 | 12 | 0 | ✅ 100% |
| 测试数据 ZIP | 3 | 3 | 0 | ✅ 100%（7/16 重打包） |
| NVD 预热 | 3 | 3 | 0 | ✅ **100%（7/16 攻克）** |
| 上传扫描+验证 | 10 | 8 | 2 | 🟡 80%（余：UI 轮询/分页人工确认） |
| 演示环境 | 10 | 7 | 3 | 🟡 70%（余：浏览器人工过一遍） |
| 文档补齐 | 4 | 4 | 0 | ✅ 100% |
| 演示材料 | 5 | 0 | 5 | 🔴 **7/17 重点** |
| 答辩话术 | 7 | 0 | 7 | 🟡 素材已备好 |
| 演示日 | 8 | 0 | 8 | 🔴 演示当天 |
| Git 提交前清理 | 3 | 3 | 0 | ✅ **100%（7/16 完成）** |
| **合计** | **65** | **40** | **25** | **62%** |

> **明日（7/17）优先**：① 浏览器人工过全流程 + 截图（仪表盘/任务/报告/Swagger，顺带补用户手册 7 处占位）→ ② PPT → ③ 演示脚本 + 答辩话术成稿 → ④ 录 2 分钟视频备份

---

## 📎 文件清单（7/14 创建 + 7/16 更新）

| 文件 | 说明 |
|------|------|
| `DashboardController.java` | 仪表盘统计 API，按 severity 分级 |
| `OpenApiConfig.java` | Swagger/OpenAPI 配置 |
| `useStatus.js` / `useTaskPolling.js` | 状态映射 / 自动轮询 composable |
| `NotFound.vue` / `logo.svg` | 404 页面 / 占位 Logo |
| `check-nvd.bat` | NVD 缓存检查（7/16 修复判定逻辑） |
| `nvd-api.properties` | NVD Key 本地配置（已 gitignore） |
| `start-demo.bat` | 演示一键启动（7/16 改离线模式 + Key 外部化） |
| `gen-cache-properties.py` | **7/16 新增**：datafeed 镜像 cache.properties 生成 |
| `normalize-nvd-json.py` | **7/16 新增**：NVD JSON 时间戳规范化（3 位小数） |
| `data/nvd-mirror/` | **7/16 新增**：NVD 全量镜像数据 212MB（gitignore） |
| `.gitignore` | **7/16 新增**：target/node_modules/data/Key 文件等 |

---

> 📌 **明天（7/17）从截图开始**：环境已全部就绪（后端离线模式 + 前端 dev server + 3 项目数据），打开 http://localhost:3000 依次截：仪表盘 → 项目列表 → 任务列表（轮询演示）→ 任务详情 → 报告下载 → Swagger。然后专心做 PPT。
da