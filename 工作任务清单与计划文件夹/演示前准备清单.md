# 🎯 实习成果展示 — 演示前准备清单

> 创建日期：2026-07-14 ｜ 目标演示日期：待定
> 最后更新：2026-07-14 23:00（第9周 Day1）
> 使用方式：逐项打勾，确保演示前每一项都是 ✅

---

## 📋 今日（7/14）成果摘要

| 类别 | 完成项 | 状态 |
|------|--------|------|
| 前端 Bug 修复 | G2-02 / G4-02 / G4-03 / G4-04 / G4-05 / G4-01 | ✅ 6/6 |
| 代码清理 | G3-01~03 / G1-01 + 死代码死依赖移除 | ✅ |
| 后端增强 | DashboardController（按severity分级）+ GET /api/tasks 分页 + open-vulnerability-clients 5.0.1→7.1.0 | ✅ |
| 测试验证 | 67/67 通过，前端 build 成功 | ✅ |
| 文档产出 | 用户手册 / 项目总览 / 报告格式说明 / Swagger API | ✅ 4 份 |
| 测试数据 | 三个 ZIP 包已打包就绪 | ✅ |
| NVD 预热 | Key 验证通过（curl → 200），DC 引擎从 403→520（认证通过但 Cloudflare 拒绝 VPN IP） | ⏳ 待换 VPN 节点 |
| 辅助脚本 | `check-nvd.bat` NVD 缓存检查脚本 | ✅ |

---

## 一、代码就绪 ✅（7/14 已完成）

- [x] **G2-02** 仪表盘漏洞按 severity 分级统计 → 新建 `DashboardController`，CRITICAL/HIGH/MEDIUM/LOW 独立显示
- [x] **G4-02** 扫描任务自动轮询 → `useTaskPolling` composable，Tasks/TaskDetail 页自动刷新
- [x] **G4-03** 前端表格分页 → Projects / Tasks / Reports 三分页全对接
- [x] **G4-01** `statusType`/`statusText` 抽取为 `useStatus` composable，4 个组件统一引用
- [x] **G4-04** 菜单项"系统设置"/"退出登录"添加 click 处理 → `ElMessage.info('功能开发中')`
- [x] **G4-05** 添加 404 路由 + `NotFound.vue`
- [x] **G3-01~03** 移除死依赖（echarts / vue-echarts / sass）和死代码（reportApi.exists/getFormats）
- [x] **G1-01** 清理 `findByNameAndIdNot` 死代码
- [x] **后端测试** 67/67 通过，零回归
- [x] **前端 build** 成功，无报错
- [x] **缺失端点** 补充 `GET /api/tasks` 分页列表接口 + `ScanTaskService.listTasks()`
- [x] **logo.svg** 补充占位 logo，修复前端构建失败

---

## 二、测试数据准备 🟡

> 演示时仪表盘和列表不能是空的！必须提前准备好有内容的测试数据。

### 现状

| 资源 | 状态 |
|------|------|
| 三个测试项目源码 | ✅ 本地已有 |
| 三个 ZIP 包 | ✅ 已打包（7/14） |
| Web 应用测试数据 | ❌ 数据库和上传目录为空 |
| NVD 缓存 | ❌ 未下载（见下方 NVD 专题） |

---

### Step 1：打包三个测试项目为 ZIP ✅ 已完成

| 文件 | 大小 | 路径 |
|------|------|------|
| `spring-petclinic.zip` | 61 MB | 项目根目录 |
| `dvba.zip` | 67 KB | 项目根目录 |
| `nacos-example.zip` | 115 KB | 项目根目录 |

---

### Step 2：启动后端和前端

> 🔑 **NVD API Key** 已获取且验证有效。当前 `application.yml` 中 Key 作为 `${NVD_API_KEY:...}` 默认值，未来需移除（不提交 Git）。

```bash
# ===== 终端 1：启动后端 =====
cd dependency-check-web
mvn spring-boot:run
# Key 已通过 application.yml 默认值注入，无需额外配置
# 看到 "Started DependencyCheckWebApplication" 即就绪

# ===== 终端 2：启动前端 =====
cd dependency-check-web/frontend
npm run dev
# 看到 "Local: http://localhost:3000/" 即就绪
```

---

### 🔴 NVD 专题：今日诊断与明日行动

> **今日结论**：API Key 有效（curl 直连 200），但 DC 引擎发出的请求被 NIST Cloudflare 拒绝。

| 尝试 | 配置 | 错误码 | 结论 |
|------|------|--------|------|
| 1-4 | 各种 Key 注入方式 | **403** | Key 未生效或格式问题 |
| 5 | 升级 open-vulnerability-clients 5.0.1→7.1.0 | **403** | 库版本不是根因 |
| 6 | Key 写入 application.yml 默认值 + VPN 连接 | **520** | ⚡ **认证通过！Cloudflare 拒绝 VPN IP** |

**明日操作**：

```bash
# 1. 换 VPN 节点（日本/新加坡/美国等）
# 2. 确认 VPN 连接后，删除旧数据库重新启动
rm -f dependency-check-web/data/dependency-check.mv.db
cd dependency-check-web
mvn spring-boot:run

# 3. 上传项目 + 发起扫描（触发 NVD 下载）
curl -X POST http://localhost:8080/api/projects \
  -F "file=@../实习专用/spring-petclinic/pom.xml" \
  -F "name=_NVD_WarmUp"
curl -X POST "http://localhost:8080/api/tasks?projectId=1"

# 4. 每 30s 查状态，直到 COMPLETED
curl http://localhost:8080/api/tasks/1

# 5. 验证缓存
check-nvd.bat
# 预期：[OK] NVD 缓存已就绪
```

- [ ] **换 VPN 节点重试**，HTTP 520 → 200
- [ ] **预热扫描完成**：状态 = COMPLETED，`vulnerableDependencies > 0`
- [ ] **NVD 缓存验证**：`check-nvd.bat` 输出 `[OK]`

---

### Step 3：通过 Web UI 逐个上传 + 扫描（NVD 缓存就绪后 ~5 分钟）

打开 `http://localhost:3000`，按顺序操作：

| 序号 | 文件 | 名称 | 预估耗时 | 预期结果 |
|------|------|------|----------|----------|
| 1 | `spring-petclinic.zip` | Spring PetClinic | 上传 5s + 扫描 ~30s | 200+ 依赖，3-5 漏洞 |
| 2 | `dvba.zip` | Damn Vulnerable Boot | 上传 3s + 扫描 ~1min | 100+ 依赖，5-10 漏洞 |
| 3 | `nacos-example.zip` | Nacos Example | 上传 5s + 扫描 ~1min | 150+ 依赖，2-4 漏洞 |

**操作路径**：项目管理 → 上传项目 → 填名称 → 选 ZIP → 上传并创建 → 开始扫描

### Step 4：验证

- [ ] **扫描全部完成**：三个任务均为 ✅ COMPLETED
- [ ] **报告可下载**：每个任务 HTML / Excel / PDF 均正常
- [ ] **仪表盘数据**：totalProjects=3, totalTasks≥3, totalVulnerabilities>0, 四等级各不相同
- [ ] **自动轮询生效**：创建新扫描时显示「🔄 自动刷新中」
- [ ] **分页生效**：列表底部有分页控件

---

## 三、演示环境验证 🔴

- [ ] **Java 环境**：`java -version` → JDK 17+
- [ ] **Maven 环境**：`mvn -version` → 能正常下载依赖
- [ ] **Node 环境**：`node -v` → 18+
- [ ] **端口检查**：8080 / 3000 未被占用
- [ ] **后端启动**：30s 内无报错
- [ ] **前端启动**：无白屏
- [ ] **浏览器兼容**：Chrome / Edge 正常
- [ ] **全流程走通**：上传 → 扫描 → 结果 → 报告下载 → 仪表盘
- [ ] **自动轮询验证**：RUNNING 任务进度条自动更新
- [ ] **分页验证**：列表底部有分页

### 现场应急

| 问题 | 措施 |
|------|------|
| 端口占用 | `netstat -ano \| findstr 8080` → `taskkill /PID xxx` |
| 后端启动失败 | 删 `data/dependency-check.mv.db` 重试 |
| NVD 损坏 | 删 `data/dc-cache/` 重新同步 |
| **终极备份** | 提前录 2 分钟屏幕录像 |

---

## 四、文档补齐 ✅（4/4 完成）

- [x] **《系统用户使用手册》** — 9 章节 + FAQ + 操作速查表，7 处截图占位待补
- [x] **项目总览文档** — 9 章：背景/定位/架构/功能/数据/亮点/完成度/交付物/限制
- [x] **《依赖分析报告格式说明》** — 7 章 + 附录：生成方式/内容结构/API/存储策略/对比
- [x] **Swagger API 文档** — springdoc-openapi 2.6.0，`/swagger-ui.html` 可访问

**PRD 要求的 11 份文档对照**：

| # | 文档 | 状态 |
|---|------|------|
| 1 | 《OWASP Dependency-Check 本地环境搭建手册》 | ✅ 已有 |
| 2 | 《OWASP Dependency-Check 核心模块原理分析手册》 | ✅ 已有 |
| 3 | 《OWASP Dependency-Check 高级使用手册》 | 🟢 低优（上游有官方文档） |
| 4 | 《集团依赖分析工具标准配置手册》 | 🟢 低优（集团内部，非实习范围） |
| 5 | 《OWASP Dependency-Check 常见问题排查手册》 | 🟢 低优（周报含踩坑记录） |
| 6 | 《集团依赖安全与合规管理最佳实践》 | 🟢 低优（同上） |
| 7 | 《系统用户使用手册》 | ✅ **7/14 完成** |
| 8 | 《轻量依赖分析Web服务开发手册》 | 🟢 低优（内容在周报+完成计划中） |
| 9 | 《前端开发手册》 | 🟢 低优（项目说明文档 §10 已覆盖） |
| 10 | 《依赖分析报告格式说明》 | ✅ **7/14 完成** |
| 11 | 项目总览文档 | ✅ **7/14 完成** + `项目说明文档.md` 已有 |

---

## 五、演示材料准备 🔴

- [ ] **PPT / 演示文稿**（10-15 页）
  - 内容大纲见 `项目总览文档.md`，每章对应 1-2 页
- [ ] **演示脚本**（逐步骤操作 + 话术）
- [ ] **架构图**（用 `项目总览文档.md` 第 3 节的 ASCII 图转 draw.io）
- [ ] **演示截图**（NVD 预热完成后截取仪表盘/任务/报告）
- [ ] **演示视频备份**（2 分钟屏幕录像）

---

## 六、答辩话术准备 🟡

- [ ] "为什么选这个项目？" → 集团痛点 + ODC 标准引擎 + 降低门槛
- [ ] "你做了什么vs别人做了什么？" → 独立完成全栈 + 引擎作为 library 引入
- [ ] "完成度为什么 99%？" → 核心 100%，22 项剩余是外围打磨
- [ ] "为什么没做认证？" → 聚焦核心能力，预留扩展点
- [ ] "为什么 H2 不是 MySQL？" → 零依赖开箱即用，MyBatis-Plus 已屏蔽差异
- [ ] "最大技术难点？" → @Async 代理 / Zip Slip 三层 / OSS 条件降级（三选一）
- [ ] "实际价值？" → 统一标准 + 降低门槛 + 完整文档

---

## 七、演示日清单 🔴

### 演示前（提前 30 分钟）
- [ ] 接电源、关无关程序
- [ ] 启动后端 + 前端，确认正常
- [ ] 确认仪表盘有数据（三个项目 + 漏洞统计）
- [ ] 打开 HTML 报告 tab 备用
- [ ] 打开 PPT / 演示脚本备用
- [ ] 清理浏览器历史

### 演示中（15-20 分钟）
- [ ] 开场：自我介绍 + 项目名称（30s）
- [ ] PPT：背景 → 架构 → 功能（5min）
- [ ] 现场演示：上传 → 扫描 → 结果 → 报告 → 仪表盘（8min）
- [ ] 技术亮点讲解（3min）
- [ ] 总结 + Q&A（3min）

### 演示后
- [ ] 关闭服务
- [ ] 提交最终代码
- [ ] 打包交付物：代码 + 文档 + PPT

---

## 八、⚠️ 提交前必做

> 以下改动涉及临时测试配置，提交 Git 前需处理！

| 文件 | 问题 | 处理 |
|------|------|------|
| `application.yml` | Key 作为 `${NVD_API_KEY:...}` 默认值 | 提交前还原为 `${NVD_API_KEY:}`（空默认） |
| `nvd-api.properties` | 含明文 Key | 已 gitignore，确认不提交 |
| `pom.xml` | open-vulnerability-clients 升级至 7.1.0 | **建议保留**（修复 NVD 兼容性，67 测试通过） |

---

## 📊 进度总览

| 大类 | 总项 | 已完成 | 待完成 | 状态 |
|------|------|--------|--------|------|
| 代码就绪 | 12 | 12 | 0 | ✅ 100% |
| 测试数据 ZIP | 3 | 3 | 0 | ✅ 100% |
| NVD 预热 | 3 | 0 | 3 | 🔴 0%（换 VPN 节点） |
| 上传扫描+验证 | 10 | 0 | 10 | 🔴 依赖 NVD |
| 演示环境 | 10 | 0 | 10 | 🔴 |
| 文档补齐 | 4 | 4 | 0 | ✅ 100% |
| 演示材料 | 5 | 0 | 5 | 🔴 |
| 答辩话术 | 7 | 0 | 7 | 🟡 |
| 演示日 | 8 | 0 | 8 | 🔴 |
| Git 提交前清理 | 3 | 0 | 3 | ⚠️ |
| **合计** | **65** | **19** | **46** | **29%** |

> **明日（7/15）优先**：① 换 VPN 节点完成 NVD 预热 → ② 上传 3 个项目 + 验证 → ③ 截仪表盘/报告图 → ④ 开始做 PPT

---

## 📎 今日新增文件清单

| 文件 | 说明 |
|------|------|
| `DashboardController.java` | 仪表盘统计 API，按 severity 分级 |
| `OpenApiConfig.java` | Swagger/OpenAPI 配置 |
| `useStatus.js` | 状态映射 composable |
| `useTaskPolling.js` | 扫描任务自动轮询 composable |
| `NotFound.vue` | 404 页面 |
| `logo.svg` | 占位 Logo |
| `check-nvd.bat` | NVD 缓存检查脚本 |
| `nvd-api.properties` | NVD Key 本地配置（不提交 Git） |
| `《系统用户使用手册》.md` | 用户操作手册 |
| `项目总览文档.md` | 项目总览 |
| `《依赖分析报告格式说明》.md` | 三格式报告说明 |
| 修改：`Dashboard.vue`、`Tasks.vue`、`TaskDetail.vue`、`Projects.vue`、`Reports.vue`、`ProjectDetail.vue`、`App.vue`、`api/index.js`、`task.js`、`project.js`、`router/index.js`、`package.json`、`pom.xml`、`application.yml`、`ScanTaskController.java`、`ScanTaskService.java`、`ProjectMapper.java` 等 |

---

> 📌 **明天从 NVD 预热开始**：连 VPN → 删旧 DB → 启动后端 → 上传上传单文件 → 发起扫描 → 等待 COMPLETED → `check-nvd.bat` 确认 [OK]。NVD 缓存就绪后，其余步骤（上传 3 项目 + 截屏 + PPT）水到渠成。
