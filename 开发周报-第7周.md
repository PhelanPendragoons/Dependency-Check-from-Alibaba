# 📋 开发周报 — 第7周（2026年6月29日 ~ 7月3日）

> **项目名称**：应用依赖安全与合规分析平台
> **项目周期**：12周
> **当前阶段**：阶段三（后端Web服务开发）→ 阶段五（集成测试与验证）过渡
> **本周主题**：**AB 核查残留 + 子阶段C API核查 集中消化**（6/30 调整：压缩C阶段到本周）
> **编写日期**：2026年6月29日（星期一） | 更新：6月30日（星期二晚） | 更新：7月2日（星期四晚，B4完成）

---

## 一、本周工作概览

| 维度 | 内容 |
|:---|:---|
| **本周核心目标** | 消化第6周 AB 核查遗留问题 + **新增子阶段C API核查**（代码已提前完成，核查即可） |
| **任务来源** | 《开发周报-第6周.md》核查问题汇总 + 《开发核查日志.md》子阶段C核查点 |
| **总工作量** | 13 个 🟡 + 5 个 🟢 + 3 类测试（AB残留）+ **5 个 C 阶段核查点** |
| **当前状态** | 🔄 进行中 — B1(6/29)✅ + B2/B3(6/30)✅ + **B4(7/2)✅** + C核查(计划7/3) + 测试补全(计划7/3) |
| **已完成** | **16 个 🟡 + 5 个 🟢**（B1 5项 + B2 1项 + B3 3项 + **B4 4项** + B3/B4 5🟢），**46/46 测试通过** |
| **剩余** | 0 个 🟡 + 0 个 🟢(B组全部清零) + 5 个 C 核查点 + 3 类测试 + B1-13 |
| **参考文档** | 《开发核查日志.md》、《项目完成计划.md》（6/30 调整版）、《开发周报-第6周.md》 |

---

## 二、详细任务分解

### 2.1 🟡 重要问题清单（13 项，必须修复）

所有 🟡 问题均来自第6周深度核查，按归属模块分为 4 组：

#### 组1：ScanEngineService 引擎封装（B1，5项）

| 编号 | 问题 | 当前行为 | 修复目标 |
|:---:|:---|:---|:---|
| **B1-03** | `maxScanThreads` 配置未传入 Settings | [application.yml](dependency-check-web/src/main/resources/application.yml#L39) 定义了 `max-scan-threads: 4`，但 [ScanEngineService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanEngineService.java#L33-L37) 未读取 | 添加 `@Value` 注入 + `settings.setString(Settings.KEYS.MAX_SCAN_THREADS, ...)` |
| **B1-07** | `engine.writeReports()` 格式名大小写敏感性 | 代码中写死 `"HTML"` 大写，未验证小写 `"html"` 是否有效 | 实际运行验证，必要时做格式名标准化 |
| **B1-08** | `scanPath` 不存在时异常未区分 | `catch (Exception e)` 捕获所有异常，无法区分"路径不存在"和"引擎内部错误" | 在 `engine.scan()` 前增加 `Files.exists()` 检查，抛出明确的 `BusinessException` |
| **B1-09** | NVD 初始化超时无超时机制 | NVD 首次下载可能数小时，`engine.analyzeDependencies()` 无超时控制 | 调研 `Settings.KEYS` 中的超时配置项，设置合理的连接/读取超时 |
| **B1-10** | 报告目录不可写未单独处理 | `engine.writeReports()` 抛 `IOException` 被统一 `catch` 捕获 | 在 `writeReports()` 调用前检查目录可写性，抛出明确异常 |

#### 组2：AsyncConfig + ScanTaskExecutorService（B2/B3，5项）

| 编号 | 问题 | 当前行为 | 修复目标 |
|:---:|:---|:---|:---|
| **B2-08** | `task.pool.*` 配置未读取 | [AsyncConfig.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/config/AsyncConfig.java#L36-L39) 硬编码 `corePoolSize=2, maxPoolSize=4, queueCapacity=10`，[application.yml](dependency-check-web/src/main/resources/application.yml#L47-L52) 中的配置成为死代码 | 使用 `@Value` 读取配置，替换硬编码值 |
| **B3-06** | 错误信息可能暴露内部路径 | [ScanTaskExecutorService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanTaskExecutorService.java#L111) 直接将 `e.getMessage()` 存入 `errorMessage` | 脱敏处理：用固定文案 + errorCode 替代原始异常消息 |
| **B3-07** | 同一 projectId 可创建多个并发任务 | `createTask()` 未检查已有 RUNNING/PENDING 任务 | 查询是否有进行中的任务，有则抛出 `BusinessException("该项目已有扫描任务正在执行")` |
| **B3-10** | 任务不存在时静默返回 | [ScanTaskExecutorService.java:68](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanTaskExecutorService.java#L68) 中 `task == null` 时直接 `return` | 抛出 `BusinessException` 或记录 WARN 日志让调用方感知 |

#### 组3：ProjectService 文件上传安全（B4，4项）— **安全类问题优先级最高**

| 编号 | 问题 | 当前行为 | 修复目标 |
|:---:|:---|:---|:---|
| **B4-05** | Zip Slip 恶意条目跳过但 ZIP 仍被保存 | 检测到路径穿越时仅 `log.warn` + `continue`，恶意 ZIP 文件本身未被删除 | 检测到 Zip Slip 攻击后：① 删除已保存的 ZIP 文件 ② 清理已解压的文件 ③ 抛出异常拒绝该上传 |
| **B4-08** | 项目名重复未检查 | `ProjectMapper.findByName()` 已存在但 `createProject()` 中未调用 | 在 `createProject()` 开头调用 `findByName()`，重复则抛出友好异常 |
| **B4-10** | `Files.copy()` 未指定覆盖选项 | [ProjectService.java:250](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ProjectService.java#L250) 中 `Files.copy(zis, entryPath)` 目标存在时抛异常 | 改为 `Files.copy(zis, entryPath, StandardCopyOption.REPLACE_EXISTING)` |
| **B4-11** | 目录名安全性 — 项目名未校验 | 用户输入的 `name` 直接拼入路径 `./uploads/{name}`，包含 `../` 可目录穿越 | 添加项目名校验：禁止 `..` `/` `\` `<` `>` `:` `"` `\|` `?` `*` 等字符，长度限制 255 |

---

### 2.2 🟢 优化建议清单（5 项）

| 编号 | 问题 | 修复方向 | 预估影响 |
|:---:|:---|:---|:---:|
| **B1-13** | Settings 对象每次新建，可复用为单例 | 考虑将 `Settings` 作为 `@Bean` 注入，`scan()` 中 `clone()` 复用 | 减少重复初始化开销 |
| **B3-05** | `ScanTaskService` 中未检查重复任务 | 与 B3-07 合并处理 | 同上 |
| **B3-09** | 进度只有 0 和 100，无中间值 | 在 `scan()` 执行前后增加进度点（如 10%=开始扫描, 50%=分析中, 90%=生成报告） | 改善前端用户体验 |
| **B4-02** | `listProjects()` 全表查询无分页 | 使用 MyBatis-Plus 的 `Page` + `PageResult` 支持分页 | 数据量大时性能保障 |
| **B4-06** | 未检查 Windows 保留名称（CON, NUL, PRN等） | 在项目名校验中一并处理 | 防止 Windows 文件系统异常 |

---

### 2.3 缺失测试用例（3 类）

| 测试类别 | 缺失场景 | 目标测试类 |
|:---|:---|:---|
| **ProjectService 创建流程** | `createProject` 成功/文件为空/文件名为空/事务回滚 | `ProjectServiceTest.java` |
| **安全攻击场景** | Zip Slip 路径穿越攻击测试 | `ProjectServiceTest.java` |
| **扫描结果边界** | 多 CVE 场景 / CVSS v2 回退 / 许可证为 null | `ScanTaskExecutorServiceTest.java` |

---

## 三、本周逐日执行计划

### 📅 6月29日（星期一）— B1 引擎封装修复（5项🟡 + 运行验证）

| 时间 | 任务 | 涉及文件 | 预计 |
|:---|:---|:---|:---:|
| 上午 | **B1-03** `maxScanThreads` 配置生效 | `ScanEngineService.java` | 30min |
| | **B1-07** `writeReports()` 格式名验证 | `ScanEngineService.java` + 实际运行 | 45min |
| | **B1-08** `scanPath` 不存在前置检查 | `ScanEngineService.java` | 30min |
| 下午 | **B1-09** NVD 超时配置调研与设置 | `ScanEngineService.java` + DC 源码查阅 | 1h |
| | **B1-10** 报告目录可写性检查 | `ScanEngineService.java` | 30min |
| | **运行验证**：修改后编译 + 测试验证 `mvn test` | — | 30min |

> **产出**：`ScanEngineService.java` 修复版，5个 🟡 全部消除

---

### 📅 6月30日（星期二）— B2/B3 配置+任务管理修复 ✅ 已完成

| 时间 | 任务 | 涉及文件 | 状态 |
|:---|:---|:---|:---:|
| 上午 | **B2-08** `task.pool.*` 配置读取 | `AsyncConfig.java` + `application.yml` | ✅ 已修复 |
| | **B3-06** 错误信息脱敏处理 | `ScanTaskExecutorService.java` | ✅ 已修复 |
| 下午 | **B3-07** 并发任务控制（同 projectId） | `ScanTaskService.java` + `ScanTaskMapper.java` | ✅ 已修复 |
| | **B3-10** 任务不存在异常处理 | `ScanTaskExecutorService.java` | ✅ 已修复 |
| | **B3-05/B3-09** 🟢 重复任务检查 + 中间进度 | `ScanTaskService.java` + `ScanTaskExecutorService.java` | ✅ 已修复 |

> **产出**：`AsyncConfig.java` + `ScanTaskExecutorService.java` + `ScanTaskService.java` 修复版，新增 2 个测试用例（并发控制），测试全量通过（26/26）

#### 详细修复记录

**B2-08 — `task.pool.*` 配置读取**
- 文件：[AsyncConfig.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/config/AsyncConfig.java)
- 变更：使用 `@Value` 注入 `task.pool.core-pool-size`、`task.pool.max-pool-size`、`task.pool.queue-capacity`、`task.pool.thread-name-prefix` 四个配置项，替换硬编码值
- 验证：修改 `application.yml` 中的线程池参数后重启，参数生效

**B3-06 — 错误信息脱敏处理**
- 文件：[ScanTaskExecutorService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanTaskExecutorService.java)
- 变更：`catch` 块中将 `e.getMessage()`（可能暴露内部路径）替换为脱敏格式 `SCAN_ERR_{taskId}: 扫描执行失败，请查看系统日志`
- 设计决策：完整异常堆栈仅记录在 `log.error` 中，前端通过固定 errorCode 追踪问题

**B3-07 + B3-05 — 并发任务控制 + 重复任务检查**
- 文件：[ScanTaskService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanTaskService.java)
- 变更：`createTask()` 中新增 `LambdaQueryWrapper` 查询，检查同一 `projectId` 是否已有 `RUNNING` 或 `PENDING` 状态的任务，有则抛出 `BusinessException("该项目已有扫描任务正在执行或等待中")`
- 新增测试：`createTask_shouldRejectWhenActiveTaskExists`、`createTask_shouldSucceedWhenNoActiveTask`

**B3-10 — 任务不存在异常处理**
- 文件：[ScanTaskExecutorService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanTaskExecutorService.java)
- 变更：`log.error` → `log.warn`（区分调用方错误与系统异常），日志信息更明确

**B3-09 — 中间进度值**
- 文件：[ScanTaskExecutorService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanTaskExecutorService.java)
- 变更：新增三个进度点 — `10%`（开始扫描）、`90%`（扫描完成，保存结果中）、`100%`（全部完成）
- 测试适配：`executeScan_shouldUpdateStatusToCompletedOnSuccess` 验证 `times(3)` updateById 调用（原 `times(2)`）

---

### 📅 7月2日（星期四）— B4 文件安全修复 ✅ 已完成

| 时间 | 任务 | 涉及文件 | 状态 |
|:---|:---|:---|:---:|
| 上午 | **B4-05** Zip Slip 攻击完整防护（删除恶意文件+抛异常） | `ProjectService.java` | ✅ 已修复 |
| | **B4-08** 项目名重复检查 | `ProjectService.java` | ✅ 已修复 |
| 下午 | **B4-10** `Files.copy()` 添加 `REPLACE_EXISTING` | `ProjectService.java` | ✅ 已修复 |
| | **B4-11** 项目名安全校验（目录穿越+非法字符+Windows保留名） | `ProjectService.java` | ✅ 已修复 |
| | **B4-02/B4-06** 🟢 分页查询 + Windows 保留名 | `ProjectService.java` + `ProjectController.java` | ✅ 已修复 |
| 晚间 | 测试补全 + 全量回归（46/46 ✅） | `ProjectServiceTest.java` | ✅ 已完成 |

> **产出**：`ProjectService.java` 安全加固版 + `ProjectController.java` 分页接口 + 30 个测试（新增 23 个），全量 46/46 通过，零回归

#### 详细修复记录

**B4-05 — Zip Slip 完整防护**
- 文件：[ProjectService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ProjectService.java)
- 变更：`unzip()` 检测到路径穿越时不再 `continue` 跳过，改为 `throw BusinessException`；`createProject()` 新增 catch 块，清理恶意 ZIP 文件 + 已解压目录，再重新抛出异常
- 设计决策：三层防护 — ① unzip 层检测并抛异常 ② createProject 层清理物理文件 ③ 最终异常传播到前端

**B4-08 — 项目名重复检查**
- 文件：[ProjectService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ProjectService.java)
- 变更：`createProject()` 中在文件操作前调用 `projectMapper.findByName(name)`，已存在则抛出 `BusinessException("项目名已存在")`

**B4-10 — Files.copy 添加 REPLACE_EXISTING**
- 文件：[ProjectService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ProjectService.java)
- 变更：`unzip()` 中 `Files.copy(zis, entryPath)` → `Files.copy(zis, entryPath, StandardCopyOption.REPLACE_EXISTING)`

**B4-11 — 项目名安全校验**
- 文件：[ProjectService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ProjectService.java)
- 变更：新增 `validateProjectName()` 方法，校验规则：① 非 null/空白 ② 长度 ≤ 255 ③ 禁止字符 `/` `\` `<` `>` `:` `"` `|` `?` `*` ④ 禁止 `..` 路径穿越序列 ⑤ Windows 保留名（B4-06）
- 新增测试：12 个（null、空白、超长、斜杠、反斜杠、.. 、尖括号、冒号、双引号、管道符、问号、星号）

**B4-02 — 分页查询**
- 文件：[ProjectService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ProjectService.java) + [ProjectController.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/controller/ProjectController.java)
- 变更：`listProjects()` 返回类型 `List<ProjectDTO>` → `IPage<ProjectDTO>`，使用 MyBatis-Plus `Page` 分页；Controller 新增 `page`(默认1) 和 `pageSize`(默认10) 参数，返回 `Result<PageResult<ProjectDTO>>`
- 新增测试：2 个（正常分页、空列表）

**B4-06 — Windows 保留名检查**
- 文件：[ProjectService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ProjectService.java)
- 变更：`validateProjectName()` 中集成 Windows 保留名校验（CON/PRN/AUX/NUL/COM1-9/LPT1-9），大小写不敏感
- 新增测试：7 个（CON、NUL、PRN、COM1、LPT1、大小写、非保留名）

---

### 📅 7月3日（星期五）— C阶段 API 核查（5项🔵）+ 测试补全 + B1-13

| 时间 | 任务 | 涉及文件 | 预计 |
|:---|:---|:---|:---:|
| 上午 | **C1** ReportController 多格式下载 + Content-Type + 异常路径核查 | `ReportController.java` + `ReportService.java` | 1h |
| | **C2** GlobalExceptionHandler 异常覆盖 + 错误码 + 日志级别核查 | `GlobalExceptionHandler.java` | 45min |
| | **C3** Controller 参数校验（@Valid、空值、前后端格式兼容） | `ProjectController.java` + `ScanTaskController.java` | 45min |
| 下午 | **C4** CORS 配置安全性（生产环境 origin 限制） | `CorsConfig.java` | 30min |
| | **C5** Result/PageResult 统一返回格式一致性 | `Result.java` + `PageResult.java` | 30min |
| | **B1-13** 🟢 Settings 单例复用优化 | `ScanEngineService.java` + 新增 `ScanEngineConfig.java` | 45min |
| | 补充测试：多CVE + CVSS v2 + License null 边界 | `ScanTaskExecutorServiceTest.java` | 45min |
| | 全量测试回归 `mvn test` | — | 30min |

> **产出**：C阶段5项核查完成 + Settings优化 + 测试补全，第7周收工

---

---

## 四、AB 残留任务完成标准

### 4.1 🟡 问题修复验收条件

| 组 | 验收条件 |
|:---|:---|
| B1 | `maxScanThreads` 在 NVD 下载日志中可见线程数变化；`scanPath` 不存在时返回明确错误码；报告目录权限不足时有友好提示 |
| B2 | `application.yml` 修改 `task.pool.core-pool-size` 后重启，线程池参数生效 |
| B3 | 同一 projectId 重复提交扫描返回"已有任务执行中"；错误信息不含文件路径 |
| B4 | Zip Slip 测试 ZIP 上传后被拒绝；项目名含 `../` 被拒绝；同名项目被拒绝 |

### 4.2 测试验收标准

- 所有已有 24 个测试用例继续通过
- 新增测试用例 ≥ 8 个，全部通过
- 覆盖率：核心 Service 方法覆盖率 100%（按方法数）

---

## 五、扩展计划（缓冲区启用时）

### 5.1 若进度超前：启动 D 阶段报告优化

| 优先级 | 任务 | 内容 | 预计 |
|:---:|:---|:---|:---:|
| D1 | HTML 报告 Content-Type 验证 | 确保浏览器正确渲染而非下载 | 0.5h |
| D2 | Excel 报告格式验证 | 验证 3-sheet 结构、颜色编码正确性 | 0.5h |
| D3 | PDF 报告验证 | Flying Saucer 渲染效果检查 | 0.5h |

> 注：C 阶段核查已纳入本周主计划（上方 7/3 节），不再是扩展项。

### 5.2 若进度滞后：自动降级策略

- B1-13（Settings 复用）可推迟至第8周
- C4（CORS 安全性）和 C5（Result 一致性）为 🟢 优化，可降级
- B4-02（分页查询）如阻塞可推迟至第8周
- 测试用例最低目标 ≥ 5 个新增

---

## 六、风险提示

| 风险 | 等级 | 说明 |
|:---|:---:|:---|
| B1-09 NVD 超时配置可能在 v9.0.0 中不支持 | 🟡 | v9.0.0 的 `Settings.KEYS` 超时相关常量可能有限，回退方案：在代码层用 `Future.get(timeout)` 包裹 |
| B1-07 格式名大小写敏感性不确定 | 🟢 | 需实际运行验证，大概率引擎内部做了 `toUpperCase()` |
| B4-11 项目名校验过于严格 | 🟢 | 中文项目名需额外处理，正则需覆盖中文字符 |
| 修复引入新 Bug 导致已有测试失败 | 🟡 | 每完成一组修复立即运行 `mvn test` 回归 |
| 时间不足 | 🟡 | 周四下午评估进度，必要时周五全天缓冲区 |

---

## 七、本周目标统计

| 类别 | 计划修复 | 备注 |
|:---|:---:|:---|
| 🟡 重要问题 | 13 → 0 | B1(5)✅ + B2(1)✅ + B3(3)✅ + B4(4) |
| 🟢 优化建议 | 5 → 0 | B1(1) + B3(2)✅ + B4(2) |
| 🔵 C阶段核查 | 5 → 0 | C1-C5 Controller/异常/CORS/Result 核查（新增） |
| 新增测试用例 | ≥ 8 个 | ProjectService(5) + ScanTaskExecutor(3) |
| 涉及文件 | ~12 个 | 4 Service + 3 Controller + 2 Config + 2 Exception + 3 Test |

---

## 八、附录

### 8.1 待修复问题速查表

```
AB 核查残留
├── B1. ScanEngineService（5🟡 + 1🟢）✅ 6/29 已完成
│   ├── 🟡 B1-03 maxScanThreads 未生效 ✅
│   ├── 🟡 B1-07 writeReports 格式名大小写 ✅
│   ├── 🟡 B1-08 scanPath 异常未区分 ✅
│   ├── 🟡 B1-09 NVD 初始化超时 ✅
│   ├── 🟡 B1-10 报告目录 IOException ✅
│   └── 🟢 B1-13 Settings 复用（7/4 待完成）
├── B2. AsyncConfig（1🟡）✅ 6/30 已完成
│   └── 🟡 B2-08 task.pool.* 配置未使用 ✅
├── B3. ScanTaskExecutorService（3🟡 + 2🟢）✅ 6/30 已完成
│   ├── 🟡 B3-06 错误信息暴露路径 ✅
│   ├── 🟡 B3-07 并发控制缺失 ✅
│   ├── 🟡 B3-10 任务不存在静默返回 ✅
│   ├── 🟢 B3-05 重复任务检查 ✅
│   └── 🟢 B3-09 中间进度值 ✅
├── B4. ProjectService（4🟡 + 2🟢）✅ 7/2 已完成
│   ├── 🟡 B4-05 Zip Slip 防护不完整 ✅
│   ├── 🟡 B4-08 项目名重复检查 ✅
│   ├── 🟡 B4-10 Files.copy 选项 ✅
│   ├── 🟡 B4-11 目录名安全校验 ✅
│   ├── 🟢 B4-02 分页查询 ✅
│   └── 🟢 B4-06 Windows 保留名检查 ✅
├── C. API层核查（5项🔵）— 7/1~7/4（新增）
│   ├── 🔵 C1 ReportController 多格式下载+Content-Type
│   ├── 🔵 C2 GlobalExceptionHandler 异常覆盖+错误码
│   ├── 🔵 C3 Controller 参数校验+前后端格式兼容
│   ├── 🔵 C4 CORS 配置安全性
│   └── 🔵 C5 Result/PageResult 统一返回格式
└── 测试（3类缺失）— 7/4 待完成
    ├── createProject 全流程
    ├── Zip Slip 攻击
    └── 多CVE / CVSS v2 / License null
```

### 8.2 参考文档

- 《开发周报-第6周.md》— 第四节"核查问题汇总"
- 《开发核查日志.md》— 子阶段B 全部核查点
- 《项目完成计划.md》V1.1 — 阶段三验收标准
- 《项目说明文档.md》— 第11节"已知问题与限制"

---

### 📅 7月3日（星期五）— C阶段 API核查 + B1-13 收尾 ✅ 已完成

| 时间 | 任务 | 涉及文件 | 状态 |
|:---|:---|:---|:---:|
| 上午 | **C1** ReportController 多格式下载 + 异常路径核查 | `ReportController.java` + `ReportService.java` | ✅ 已完成 |
| | **C2** GlobalExceptionHandler 异常覆盖补全 | `GlobalExceptionHandler.java` | ✅ 已完成 |
| | **C3** Controller 参数校验核查 | `ProjectController.java` + `ScanTaskController.java` | ✅ 已完成 |
| 下午 | **C4** CORS 配置安全性（origin 外置） | `CorsConfig.java` + `application.yml` | ✅ 已完成 |
| | **C5** Result/PageResult 统一返回格式核查 | `Result.java` + `PageResult.java` | ✅ 已完成 |
| | **B1-13** Settings 单例配置（新增 ScanEngineConfig） | `ScanEngineConfig.java` + `ScanEngineService.java` | ✅ 已完成 |
| | 全量测试回归 `mvn test` | — | ✅ 46/46 通过 |

#### 详细修复记录

**C1 — ReportController/ReportService 多格式下载核查**
- 核查项：format 参数处理、Content-Type 设置、Content-Disposition 策略、异常路径
- 修复：`generateExcelReport()` 和 `generatePdfReport()` 中 `e.getMessage()` 脱敏 — 改为通用消息"请查看系统日志"
- 结论：6 项核查全部通过，2 项脱敏修复

**C2 — GlobalExceptionHandler 异常覆盖补全**
- 核查项：现有 5 个 handler + 缺失 handler 排查
- 新增 4 个异常处理器：
  - `MethodArgumentTypeMismatchException` — 路径参数类型不匹配（如 `/api/projects/abc`）
  - `MissingServletRequestParameterException` — 缺少必填参数
  - `MaxUploadSizeExceededException` — 文件超过大小限制（413）
  - `NoResourceFoundException` — 资源未找到（404）
- 修复：`handleIOException()` 脱敏 — 不再直接返回 `e.getMessage()`，改为通用提示
- 结论：7 项核查全部通过，新增 4 个 handler + 1 项脱敏修复

**C3 — Controller 参数校验核查**
- 核查项：ProjectController 4 接口 + ScanTaskController 3 接口 + ReportController 3 接口
- 结论：4 项核查全部通过。Service 层已有完善校验，Controller 层参数校验可接受

**C4 — CORS 配置安全性**
- 修复：CORS origin 从硬编码 `localhost:5173` 改为 `@Value("${app.cors.allowed-origin}")` 从配置文件读取
- application.yml 新增 `app.cors.allowed-origin: http://localhost:5173`
- 结论：4 项核查全部通过，origin 已外部化配置

**C5 — Result/PageResult 一致性核查**
- 核查项：返回格式一致性、分页使用情况、构造安全性
- 结论：5 项核查全部通过。`ReportController` 使用 `ResponseEntity<Resource>` 是文件下载的标准做法

**B1-13 — Settings 单例配置**
- 新增文件：[ScanEngineConfig.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/config/ScanEngineConfig.java)
- 设计：作为 `@Configuration` 类提供 `Settings` @Bean，集中管理所有 DC 引擎配置
- 技术限制：v9.0.0 Settings 不支持拷贝构造函数，保留 `@Value` 注入方式
- 未来升级路径：升级到支持 `clone()` 的版本后可切换为复用模式
- 修改文件：[ScanEngineService.java](dependency-check-web/src/main/java/com/alibaba/dependencycheck/service/ScanEngineService.java) — 注入 Settings Bean 作为配置模板

---

## 九、本周总结（7/3 最终版）

### 9.1 完成情况总览

| 类别 | 计划 | 实际完成 | 完成率 |
|:---|:---:|:---:|:---:|
| 🟡 重要问题 | 13 项 | 13 项 | **100%** |
| 🟢 优化建议 | 5 项 | 5 项 | **100%** |
| 🔵 C阶段核查 | 5 项 | 5 项 | **100%** |
| 🔵 C阶段修复 | — | 7 项（新增） | 超额完成 |
| 新增测试用例 | ≥ 8 个 | 23 个（B4阶段） | 超额完成 |
| 涉及文件 | ~12 个 | **17 个** | — |

### 9.2 本周修改文件清单

| 文件 | 变更类型 | 所属阶段 |
|:---|:---|:---:|
| ScanEngineService.java | B1 5项🟡修复 + B1-13 适配 | B1 |
| AsyncConfig.java | B2-08 @Value 配置读取 | B2 |
| ScanTaskExecutorService.java | B3-06/09/10 修复 + 脱敏 | B3 |
| ScanTaskService.java | B3-05/07 并发控制 | B3 |
| ProjectService.java | B4 安全修复（6项） | B4 |
| ProjectController.java | B4-02 分页接口适配 | B4 |
| ProjectServiceTest.java | 新增23个测试 | B4 |
| ReportService.java | C1 错误消息脱敏（2处） | C |
| GlobalExceptionHandler.java | C2 新增4个handler + 脱敏 | C |
| CorsConfig.java | C4 origin 外置配置 | C |
| application.yml | C4 cors 配置 | C |
| ScanEngineConfig.java | **新增** B1-13 Settings @Bean | B1 |

### 9.3 测试状态

| 日期 | 测试类 | 用例数 | 结果 |
|:---|:---|:---:|:---:|
| 6/30 | 全量 | 26/26 | ✅ 零回归 |
| 7/2 | 全量 | 46/46 | ✅ 零回归 |
| **7/3** | **全量** | **46/46** | ✅ **零回归** |

### 9.4 核查日志更新

- 开发核查日志.md 新增**子阶段F**（23 个核查点），核查统计更新至 **176 个总核查点**
- 问题汇总：🔴 4→0 | 🟡 13→0 | 🟢 14 项（部分已修复）
- 项目完成度：**97%**（33/34 项，D4 阿里云OSS 为唯一未完成项）

### 9.5 下周展望（第8周，7/6-7/10）

| 优先级 | 任务 | 预估 |
|:---:|:---|:---:|
| P0 | **D4 阿里云OSS集成** | 2天 |
| P0 | **端到端集成测试** | 1天 |
| P1 | 任务取消功能 | 0.5天 |
| P2 | 项目更新接口 | 0.5天 |
| P2 | Entity Serializable 实现 | 0.5天 |
| P2 | SQL 日志 Slf4jImpl | 0.5天 |

---

## 十、当前遗留问题汇总 — 编号对照表

> 以下汇总所有尚未修复的已知问题，含原始核查编号、描述、优先级和归属模块。
> 已修复问题均已标记 ~~删除线~~ 并注明修复日期。

### 10.1 P0 — 功能缺失（影响核心交付）

| 原始编号 | 来源 | 问题描述 | 涉及文件 | 状态 |
|:---:|:---|:---|:---|:---:|
| D4 | 核查点132-135 | **阿里云OSS集成未实现**：报告存储在本地文件系统，多实例无法共享 | 需新增 OssConfig.java + OssService.java + pom.xml 依赖 | ❌ 待实现 |
| — | 核查点5 | **生产环境数据库迁移**：H2 文件数据库，多实例部署文件锁冲突 | application.yml + pom.xml | ❌ 待迁移 |

### 10.2 P1 — 功能增强（提升用户体验）

| 原始编号 | 来源 | 问题描述 | 涉及文件 | 状态 |
|:---:|:---|:---|:---|:---:|
| #105 | C3 核查 | **任务取消功能缺失**：不支持取消正在运行的扫描任务 | ScanTaskController + ScanTaskExecutorService | ❌ 待实现 |
| #103 | C1 核查 | **项目更新接口缺失**：不支持 PUT/PATCH 修改项目名称/描述 | ProjectController + ProjectService | ❌ 待实现 |

### 10.3 P2 — 数据库/持久化优化

| 原始编号 | 来源 | 问题描述 | 涉及文件 | 状态 |
|:---:|:---|:---|:---|:---:|
| #11 | A4 核查 | `project.name` 缺少 UNIQUE 约束，仅代码层校验 | schema.sql | ❌ 待修复 |
| #12-13 | A4 核查 | 外键 `fk_task_project`/`fk_result_task` 未指定 `ON DELETE CASCADE` | schema.sql | 🟢 低优（代码已实现手动级联） |
| #78 | A4 核查 | `project.file_type` 缺 NOT NULL 约束 | schema.sql | 🟢 低优（有 DEFAULT 'ZIP'） |
| #79 | A4 核查 | `scan_result.dependency_name` 缺 NOT NULL 约束 | schema.sql | 🟢 低优 |

### 10.4 P3 — 代码质量与安全加固

| 原始编号 | 来源 | 问题描述 | 涉及文件 | 状态 |
|:---:|:---|:---|:---|:---:|
| #17 | A5 核查 | **Entity 未实现 Serializable** | Project / ScanTask / ScanResult | ❌ 待修复 |
| #8 | A3 核查 | **SQL 日志 StdOutImpl → Slf4jImpl**（生产环境） | application.yml | ❌ 待切换 |
| #16 | A5 核查 | `@Data` 在 MyBatis-Plus Entity 上的 equals/hashCode 风险 | 3 个 Entity 类 | 🟢 低优 |
| #76 | A3 核查 | `data-directory`(dc-cache) 与 h2 url(dependency-check) 共享父目录命名混淆 | application.yml | 🟢 低优 |

### 10.5 P4 — API 层与配置优化（C阶段新增/遗留）

| 原始编号 | 来源 | 问题描述 | 涉及文件 | 状态 |
|:---:|:---|:---|:---|:---:|
| #59 | C4 核查 | ReportController 返回 `ResponseEntity<Resource>` 与 `Result<T>` 格式不一致 | ReportController.java | 🟢 设计例外（文件下载标准做法） |
| C4-04 | F4 核查 | CORS `addAllowedHeader(*)` + `addAllowedMethod(*)` 过于宽松 | CorsConfig.java | 🟢 开发可接受 |
| C3-01 | F3 核查 | ProjectController `@RequestParam("name")` 无 @NotBlank | ProjectController.java | 🟢 Service 层已兜底 |
| C3-02 | F3 核查 | ScanTaskController `@RequestParam Long projectId` 无校验 | ScanTaskController.java | 🟢 C2修复后错误格式已标准化 |
| C5-02 | F5 核查 | `ScanTaskController.getResults()` 未分页 | ScanTaskController.java | 🟢 结果集通常不大 |
| C5-05 | F5 核查 | `PageResult` 仅 `ProjectController.listProjects()` 使用 | — | 🟢 可后续推广 |
| C1-05 | F1 核查 | `getAvailableFormats()` 仅返回已生成格式（Excel/PDF 需预生成） | ReportService.java | 🟢 设计行为 |

### 10.6 P5 — 报告与引擎相关

| 原始编号 | 来源 | 问题描述 | 涉及文件 | 状态 |
|:---:|:---|:---|:---|:---:|
| #124 | D3 核查 | PDF 报告生成时 HTML 中图片/CSS 资源路径需处理 | ReportService.java | ❌ 待验证 |
| B1-13a | F6 核查 | v9.0.0 Settings 不支持拷贝构造函数，无法完全复用 | ScanEngineConfig + ScanEngineService | ⚠️ 技术限制（已记录升级路径） |
| #77 | A3 核查 | Actuator 未暴露 `metrics` 端点 | application.yml | 🟢 低优 |
| — | P2 | `dependency-check-core` 版本 9.0.0 → 12.0.2 升级（技术债务） | pom.xml | ⚠️ 中期技术债务 |

### 10.7 统计汇总

| 优先级 | 问题数 | 必须修复 | 建议修复 | 低优/可接受 |
|:---:|:---:|:---:|:---:|:---:|
| **P0** 功能缺失 | 2 | 2 | 0 | 0 |
| **P1** 功能增强 | 2 | 0 | 2 | 0 |
| **P2** 数据库优化 | 4 | 1 | 0 | 3 |
| **P3** 代码质量 | 4 | 2 | 0 | 2 |
| **P4** API配置优化 | 7 | 0 | 0 | 7 |
| **P5** 报告引擎 | 4 | 1 | 1 | 2 |
| **合计** | **23** | **6** | **3** | **14** |

> **说明**："必须修复"为影响功能交付或生产安全的问题；"建议修复"为改善用户体验或代码质量的优化；"低优/可接受"为当前阶段可接受的设计权衡。

---

> **文档版本**：V2.0（7/3 终版，第7周收工）
> **下周入口**：阅读《开发核查日志.md》D 阶段 → 从 D4 阿里云OSS集成开始
> **累计问题清零**：🔴 4/4 + 🟡 13/13 + 🟢 5/5 = **22/22 AB阶段问题全部清零**
