# 📋 开发周报 — 第6周（2026年6月22日 ~ 6月26日）

> **项目名称**：应用依赖安全与合规分析平台  
> **项目周期**：12周  
> **当前阶段**：阶段三（后端Web服务开发）— **子阶段B 全面核查**  
> **编写日期**：2026年6月23日（星期二）

---

## 一、本周工作概览

| 维度 | 内容 |
|:---|:---|
| **本周核心目标** | 阶段三-B（核心业务逻辑）全面核查 + 实际运行验证 |
| **核查范围** | 4 个 Service 类（ScanEngineService、ScanTaskService、ScanTaskExecutorService、ProjectService）+ AsyncConfig |
| **核查方式** | 代码审查 + 实际运行验证 + 边界/异常场景测试 |
| **参考文档** | 《开发核查日志.md》子阶段B、《项目完成计划.md》阶段三B验收标准 |
| **当前进度** | 阶段三-A/B/C/D 代码已完成，B 部分进入全面核查阶段 |

---

## 二、本周详细工作记录

### 📅 6月22日（星期一）— B1 ScanEngineService 引擎封装全面核查

#### 完成内容

| 任务 | 文件 | 状态 |
|:---|:---|:---:|
| Engine 生命周期管理核查（try-finally 资源释放） | `ScanEngineService.java` | ✅ |
| Settings 配置完整性核查（NVD API Key、数据目录） | `ScanEngineService.java` | ✅ |
| API 兼容性代码审查（v9.0.0） | `ScanEngineService.java` | ✅ |
| 异常处理健壮性审查（scanPath 不存在、NVD 初始化超时） | `ScanEngineService.java` | ⚠️ 发现 3 个潜在风险 |
| 线程安全分析（单例 Bean + 局部 Engine 实例） | `ScanEngineService.java` | ✅ |
| Settings 复用优化评估 | `ScanEngineService.java` | 🟢 优化建议 |
| **新增发现**：`maxScanThreads` 配置未传入 Settings | `ScanEngineService.java` + `application.yml` | ⚠️ 配置未生效 |

#### B1 核查详情

| 核查点编号 | 核查项 | 当前状态 | 备注 |
|:---:|:---|:---:|:---|
| B1-01 | `Engine` 实例在 `try` 块中创建，`finally` 块中关闭 | ✅ 通过 | 资源释放路径完整，无遗漏 |
| B1-02 | `Settings` 配置完整性（DATA_DIRECTORY + NVD_API_KEY） | ✅ 通过 | 已配置两项核心参数 |
| B1-03 | `maxScanThreads` 未传入 Settings | ⚠️ 配置未生效 | `application.yml` 中定义了 `max-scan-threads: 4`，但 `ScanEngineService` 中未读取该配置 |
| B1-04 | API 兼容性 — v9.0.0 构造函数签名 | ✅ 通过 | `Engine(Engine.Mode.STANDALONE, settings)` 签名正确 |
| B1-05 | API 兼容性 — `engine.scan(String)` | ✅ 通过 | v9.0.0 接受 String 参数 |
| B1-06 | API 兼容性 — `engine.getDependencies()` 返回类型 | ✅ 通过 | 返回 `Dependency[]` 数组，使用 `Arrays.asList()` 转换 |
| B1-07 | API 兼容性 — `engine.writeReports(String, File, String)` | ⚠️ 待运行验证 | 第三个参数接受 String 格式名，需验证大小写敏感性 |
| B1-08 | 异常处理 — `scanPath` 不存在时的异常 | ⚠️ 潜在风险 | `catch (Exception e)` 未区分具体异常类型 |
| B1-09 | 异常处理 — NVD 初始化超时 | ⚠️ 潜在风险 | 默认超时 2 分钟，当前代码没有设置超时机制 |
| B1-10 | 异常处理 — 报告目录不可写 | ⚠️ 潜在风险 | `engine.writeReports()` 会抛出 `IOException`，未单独处理 |
| B1-11 | 线程安全 — 单例 Bean + 局部 Engine 实例 | ✅ 通过 | 无竞态条件 |
| B1-12 | `Engine.Mode.STANDALONE` 模式适用性 | ✅ 通过 | 适合嵌入 Spring Boot |
| B1-13 | Settings 复用优化 | 🟢 优化建议 | 可改为 `@Bean` 单例 + `clone()` 方式复用 |

---

### 📅 6月23日（星期二）— B2 AsyncConfig + 异步任务管理核查

#### 完成内容

| 任务 | 文件 | 状态 |
|:---|:---|:---:|
| @Async 生效验证（代理对象调用） | `ScanTaskExecutorService.java` + `ScanTaskService.java` | ✅ |
| 线程池参数合理性评估 | `AsyncConfig.java` | ✅ |
| 拒绝策略验证（CallerRunsPolicy） | `AsyncConfig.java` | ✅ |
| 优雅关闭配置核查 | `AsyncConfig.java` | ✅ |
| 任务状态流转验证（PENDING→RUNNING→COMPLETED/FAILED） | `ScanTaskService.java` + `ScanTaskExecutorService.java` | ✅ |
| **新增发现**：`task.pool.*` 配置未使用 | `application.yml` + `AsyncConfig.java` | ⚠️ 配置未生效 |

#### B2 核查详情

| 核查点编号 | 核查项 | 当前状态 | 备注 |
|:---:|:---|:---:|:---|
| B2-01 | `@Configuration` + `@EnableAsync` 注解存在 | ✅ 通过 | 配置完整 |
| B2-02 | `@Bean("scanTaskExecutor")` 定义线程池 Bean | ✅ 通过 | 名称与 `@Async("scanTaskExecutor")` 对应 |
| B2-03 | 线程池参数合理性评估 | ✅ 通过 | 开发/测试环境合理，生产环境需调整 |
| B2-04 | 拒绝策略 `CallerRunsPolicy` 分析 | ✅ 通过 | 不会丢失任务，但可能阻塞 HTTP 请求线程 |
| B2-05 | 优雅关闭配置 | ✅ 通过 | `waitForTasksToComplete=true` + `awaitTerminationSeconds=60` |
| B2-06 | `@Async` 生效机制验证 | ✅ 通过 | 通过 `ScanTaskExecutorService` 代理对象调用 |
| B2-07 | 任务状态流转完整性 | ✅ 通过 | PENDING → RUNNING → COMPLETED/FAILED |
| B2-08 | `application.yml` 中 `task.pool.*` 配置未使用 | ⚠️ 配置未生效 | `AsyncConfig.java` 硬编码了线程池参数，未读取配置文件 |

---

### 📅 6月24日（星期三）— B3 ScanTaskService + ScanTaskExecutorService 核查

#### 计划内容

| 任务 | 文件 | 状态 |
|:---|:---|:---:|
| 多 CVE 支持实际运行验证 | `ScanTaskExecutorService.java` | ⏳ |
| Null 安全验证（cvssV3、license 等） | `ScanTaskExecutorService.java` | ⏳ |
| 并发控制核查（同一 projectId 多任务） | `ScanTaskService.java` | ⏳ |
| 错误信息安全性审查 | `ScanTaskExecutorService.java` | ⏳ |
| 扫描结果完整性验证 | `ScanTaskExecutorService.java` | ⏳ |
| 进度更新机制评估 | `ScanTask.java` + `ScanTaskExecutorService.java` | ⏳ |

#### B3 核查清单

| 核查点编号 | 核查项 | 预期状态 | 备注 |
|:---:|:---|:---:|:---|
| B3-01 | 多 CVE 支持 — 每个漏洞生成一条记录 | ✅ 已修复 | 当前代码已支持 |
| B3-02 | `cvssV3.getCvssData().getBaseScore()` Null 安全 | ✅ 已处理 | 检查了 `cvssV3 != null` |
| B3-03 | `result.getIsVulnerable()` 为 null 处理 | ✅ 已处理 | 代码中已处理 |
| B3-04 | `dep.getLicense()` 为 null 处理 | ✅ 已处理 | 代码中已处理 |
| B3-05 | 并发控制 — 同一 projectId 多任务 | 🟢 优化建议 | 未检查是否有正在运行的任务 |
| B3-06 | 错误信息安全性 — 可能暴露内部路径 | ⚠️ 潜在风险 | `e.getMessage()` 可能包含敏感信息 |
| B3-07 | 无漏洞依赖生成 `isVulnerable=false` 记录 | ✅ 通过 | 确保每个依赖都有记录 |
| B3-08 | 进度更新 — 中间进度值 | 🟢 优化建议 | 目前只有 0 和 100 |

---

### 📅 6月25日（星期四）— B4 ProjectService 文件上传与解压核查

#### 计划内容

| 任务 | 文件 | 状态 |
|:---|:---|:---:|
| @Transactional 事务回滚验证 | `ProjectService.java` | ⏳ |
| 级联删除验证（项目→任务→结果） | `ProjectService.java` | ⏳ |
| 物理文件清理验证 | `ProjectService.java` | ⏳ |
| Zip Slip 安全测试 | `ProjectService.java` | ⏳ |
| 文件名安全性审查 | `ProjectService.java` | ⏳ |
| 项目名重复检查 | `ProjectService.java` | ⏳ |
| 文件已存在处理审查 | `ProjectService.java` | ⏳ |

#### B4 核查清单

| 核查点编号 | 核查项 | 预期状态 | 备注 |
|:---:|:---|:---:|:---|
| B4-01 | `createProject()` 使用 `@Transactional` | ✅ 已修复 | 当前代码已添加 |
| B4-02 | `listProjects()` 全表查询 | 🟢 优化建议 | 数据量大时性能差 |
| B4-03 | `deleteProject()` 级联删除 | ✅ 已修复 | 先删结果→再删任务→最后逻辑删除项目 |
| B4-04 | `deleteProject()` 物理文件清理 | ✅ 已修复 | 已实现 `deleteDirectory()` 方法 |
| B4-05 | Zip Slip 防护 — `entryPath.startsWith(destPath)` | ✅ 已实现 | 但恶意条目被跳过时 ZIP 文件仍被保存 |
| B4-06 | 文件名安全 — Windows 保留名称检查 | 🟢 优化建议 | 未检查 `../`、`CON`、`NUL` 等 |
| B4-07 | `file.transferTo()` 覆盖行为 | ✅ 安全 | Spring Boot 的 `transferTo()` 会覆盖 |
| B4-08 | 项目名重复检查 | ⚠️ 潜在风险 | `findByName()` 存在但 `createProject()` 中未调用 |
| B4-09 | `deleteDirectory()` 递归删除 | ✅ 通过 | 使用 `Files.walk()` + `sorted(reverseOrder())` |
| B4-10 | `Files.copy()` 未指定 `StandardCopyOption` | ⚠️ 潜在风险 | 目标文件已存在时会抛出异常 |

---

### 📅 6月26日（星期五）— 验收验证 + 问题修复 + 周报编写

#### 计划内容

| 任务 | 状态 |
|:---|:---:|
| B 阶段 4 项验收标准逐项验证 | ⏳ |
| 发现的问题修复（🔴/🟡 级别） | ⏳ |
| 开发核查日志子阶段B更新 | ⏳ |
| 编写第六周开发周报 | ⏳ |

---

## 三、子阶段B 验收标准验证

### 验收标准对照表

| 编号 | 验收标准 | 验证方法 | 验证结果 | 备注 |
|:---:|:---|:---|:---:|:---|
| B-AC1 | ScanEngineService 能成功调用 DependencyCheck 引擎 | 启动项目，上传项目并触发扫描 | ⏳ 待验证 | 需实际运行 `mvn spring-boot:run` |
| B-AC2 | 异步扫描任务能正常创建和执行 | 创建任务后立即返回，后台异步执行 | ⏳ 待验证 | 需验证 @Async 是否真正异步 |
| B-AC3 | 文件上传解压功能正常 | 上传 ZIP 文件，检查解压结果 | ⏳ 待验证 | 需验证 Zip Slip 防护 |
| B-AC4 | 扫描结果能正确解析并存入数据库 | 扫描完成后查询数据库 | ⏳ 待验证 | 需验证多 CVE 支持 |

---

## 四、子阶段B 核查问题汇总

### 🔴 严重问题（待发现）

| 编号 | 文件 | 问题描述 | 风险等级 | 修复状态 |
|:---:|:---|:---|:---:|:---:|
| — | — | 待核查中发现 | 🔴 | ⏳ |

### 🟡 重要问题（待验证）

| 编号 | 文件 | 问题描述 | 风险等级 | 修复状态 |
|:---:|:---|:---|:---:|:---:|
| B3-06 | ScanTaskExecutorService.java | 错误信息可能暴露内部路径 | 🟡 | ⏳ 待验证 |
| B4-08 | ProjectService.java | 项目名重复未检查 | 🟡 | ⏳ 待验证 |
| B4-10 | ProjectService.java | Files.copy 未指定 StandardCopyOption | 🟡 | ⏳ 待验证 |

### 🟢 优化建议

| 编号 | 文件 | 问题描述 | 优先级 |
|:---:|:---|:---|:---:|
| B1-08 | ScanEngineService.java | Settings 对象可复用为单例 Bean | 低 |
| B3-05 | ScanTaskService.java | 未检查重复扫描任务 | 低 |
| B3-08 | ScanTaskExecutorService.java | 进度从未更新中间值（只有 0 和 100） | 低 |
| B4-02 | ProjectService.java | listProjects() 全表查询 | 低 |
| B4-06 | ProjectService.java | 未检查文件名中的 Windows 保留名称 | 低 |

---

## 五、项目整体进度

### 5.1 里程碑进度

| 阶段 | 对应周次 | 计划内容 | 当前状态 | 进度 |
|:---:|:---:|:---|:---:|:---:|
| 阶段一：环境准备与基础学习 | 第1周（5/26~5/29） | JDK/Maven 安装、DependencyCheck 编译运行 | ✅ 已完成 | 100% |
| 阶段二：基准引擎深度分析 | 第2周（5/31~6/4） | Engine.java 核心架构、60+ 分析器体系分析 | ✅ 已完成 | 100% |
| 阶段三-A：项目骨架搭建 | 第3周（6/5~6/7） | Spring Boot 项目初始化、Entity/DTO/VO 定义 | ✅ 已完成 | 100% |
| 阶段三-B：核心业务逻辑开发 | 第3~4周（6/8~6/10） | Service 层、Mapper 层、配置层开发 | ✅ 代码完成，**本周全面核查** | 50%→90% |
| 阶段三-C：REST API 开发 | 第4周（6/10~6/11） | Controller 层、异常处理层开发 | ✅ 已完成 | 100% |
| 阶段三-D：报告生成与存储集成 | 第4~5周（6/10~6/17） | ScanEngineService 封装、报告生成（HTML/Excel/PDF） | ✅ 已完成 | 100% |
| 阶段四：前端页面开发 | 第5~6周 | Vue 3 + Element Plus 前端页面 | ✅ 初版完成 | 100% |
| 阶段五：集成测试与验证 | 第7周 | 端到端测试、安全审查、性能测试 | ❌ 未开始 | 0% |
| 阶段六：Docker 部署 | 第8周 | 容器化部署 | ❌ 未开始 | 0% |

### 5.2 代码统计

| 指标 | 第5周（上周） | 第6周（本周） | 变化 |
|:---|:---:|:---:|:---:|
| Java 源文件 | 17 个 | 17 个 | — |
| 前端文件 | 18 个 | 18 个 | — |
| 配置文件 | 2 个 | 2 个 | — |
| SQL 脚本 | 1 个 | 1 个 | — |
| 单元测试 | 3 个测试类，24 个用例 | 3 个测试类，24 个用例 | — |
| 代码行数（后端） | ~2,800 行 | ~2,800 行 | — |
| 代码行数（前端） | ~1,500 行 | ~1,500 行 | — |

---

## 六、技术债务与风险

### 6.1 当前技术债务

| 债务项 | 影响 | 计划处理时间 |
|:---|:---|:---|
| DependencyCheck Core 版本为 v9.0.0（项目完成计划要求 v12.0.2） | 功能差异，API 可能不兼容 | 阶段五 |
| 🟢 优化建议 10+ 个未处理 | 代码质量可进一步提升 | 穿插处理 |
| 前端页面未与后端联调测试 | 可能存在接口不兼容问题 | 阶段五 |
| 缺少端到端集成测试 | 无法验证完整链路 | 阶段五 |

### 6.2 风险项

| 风险 | 等级 | 应对措施 |
|:---|:---:|:---|
| JDK 26 与 DependencyCheck 的 SSL 兼容性问题 | 🟡 | 已切换至 JDK 17 LTS |
| Sonatype OSS Index API 需要认证 | 🟡 | 需注册账号获取 API Key |
| NVD API Key 未配置时扫描功能受限 | 🟡 | 已支持从环境变量读取 |
| 前端与后端联调可能出现接口不匹配 | 🟡 | 阶段五集中处理 |

---

## 七、下周工作计划（第7周：6月29日 ~ 7月3日）

### 7.1 优先级 P0：阶段五 集成测试与验证

| 任务 | 预计工时 | 说明 |
|:---|:---:|:---|
| **P0-1**：后端编译与启动验证 | 0.5天 | `mvn spring-boot:run` 验证项目启动 |
| **P0-2**：前端 npm install + npm run dev 验证 | 0.5天 | 确保前端项目可正常启动 |
| **P0-3**：前后端联调测试 | 1天 | 验证 API 接口与前端页面的兼容性 |
| **P0-4**：端到端流程测试 | 1天 | 上传项目 → 创建扫描 → 查看结果 → 下载报告 |

### 7.2 优先级 P1：阶段三-D 收尾 + 优化

| 任务 | 预计工时 | 说明 |
|:---|:---:|:---|
| **P1-1**：修复 🟢 优化建议（10 个） | 1天 | 按优先级处理低风险优化项 |
| **P1-2**：DependencyCheck Core 升级到 v12.0.2 | 1天 | 适配新版 API |
| **P1-3**：NVD API Key + Sonatype OSS Index 配置验证 | 0.5天 | 验证完整漏洞检测能力 |

### 7.3 优先级 P2：阶段六 Docker 部署准备

| 任务 | 预计工时 | 说明 |
|:---|:---:|:---|
| **P2-1**：编写 Dockerfile | 0.5天 | 后端 + 前端容器化 |
| **P2-2**：编写 docker-compose.yml | 0.5天 | 多容器编排 |
| **P2-3**：本地 Docker 部署验证 | 1天 | 验证容器化部署 |

### 7.4 时间安排建议

| 日期 | 计划任务 |
|:---:|:---|
| 6月29日（星期一） | P0-1 ~ P0-2：后端启动验证 + 前端启动验证 |
| 6月30日（星期二） | P0-3：前后端联调测试 |
| 7月1日（星期三） | P0-4：端到端流程测试 |
| 7月2日（星期四） | P1-1：🟢 优化建议修复 + P1-2：DependencyCheck 升级 |
| 7月3日（星期五） | P2-1 ~ P2-3：Docker 部署 + 本周总结 |

---

## 八、附录

### 8.1 子阶段B 核查范围总览

```
子阶段B：核心业务逻辑开发
├── B1. ScanEngineService.java（引擎封装）
│   ├── Engine 生命周期管理
│   ├── Settings 配置完整性
│   ├── API 兼容性验证
│   ├── 异常处理健壮性
│   └── 线程安全分析
├── B2. AsyncConfig.java（异步任务线程池）
│   ├── @EnableAsync 配置
│   ├── 线程池参数合理性
│   ├── 拒绝策略
│   └── 优雅关闭
├── B3. ScanTaskService + ScanTaskExecutorService（异步扫描任务管理）
│   ├── @Async 生效验证
│   ├── 多 CVE 支持
│   ├── Null 安全处理
│   ├── 并发控制
│   └── 任务状态流转
└── B4. ProjectService.java（文件上传与解压）
    ├── @Transactional 事务管理
    ├── 级联删除
    ├── 物理文件清理
    ├── Zip Slip 防护
    ├── 文件名安全
    └── 项目名重复检查
```

### 8.2 启动命令

```bash
# 后端启动
cd dependency-check-web
mvn spring-boot:run

# 前端启动（新开终端）
cd dependency-check-web/frontend
npm install
npm run dev
```

### 8.3 参考文档

- 《OWASP Dependency-Check 核心模块原理分析手册》
- 《OWASP Dependency-Check 本地环境搭建手册》
- 《项目完成计划.md》V1.1
- 《开发核查日志.md》（2026年6月23日更新）
- 《工作日志.md》
- 《开发周报-第4周.md》
- 《开发周报-第5周.md》

---

> **文档版本**：V1.0（初版）
> **下次更新**：2026年6月26日（第6周周五）
